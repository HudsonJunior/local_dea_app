name: local_dea_app
description: A new Flutter project.

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  font_awesome_flutter: ^9.2.0
  google_maps_flutter: ^2.1.0
  intl: ^0.17.0
  geolocator: ^8.0.1
  flutter_bloc: ^8.0.1
  bloc: ^8.0.1

dev_dependencies:
  flutter_lints: ^1.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  assets:
    - assets/dea_icon.png
    - assets/edificio-hospitalar.bmp
    - assets/edificio-hospitalar.png
